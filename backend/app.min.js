const express=require("express"),cors=require("cors"),app=express(),api=require("./api/index.js");app.use("/",api);const port=5e3;app.use(cors()),app.use(express.json());const mysql=require("mysql"),connection=mysql.createConnection({host:"localhost",user:"root",password:"root"});connection.connect(),app.listen(5e3,()=>{console.log("Example app listening at http://localhost:5000")});